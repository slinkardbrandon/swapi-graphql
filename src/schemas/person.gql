# import * from './starship.gql'

type Person {
  name: String
  height: String
  mass: String
  hair_color: String
  skin_color: String
  eye_color: String
  gender: String
  starships: [Starship!]
}

type Query {
  getPerson(id: ID!): Person!
}
